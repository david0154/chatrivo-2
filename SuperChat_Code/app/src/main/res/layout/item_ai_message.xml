<?xml version="1.0" encoding="utf-8"?>
<!--
  Chat bubble item layout for Chatrivo AI messages
  Used in RecyclerView inside AIChatActivity
  - isUser=true  → right-aligned blue bubble
  - isUser=false → left-aligned purple AI bubble with avatar
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="4dp"
    android:paddingBottom="4dp"
    android:paddingStart="8dp"
    android:paddingEnd="8dp">

    <!-- AI Avatar (shown only for AI messages) -->
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/ivAvatar"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:civ_border_color="#6200EE"
        app:civ_border_width="1dp" />

    <!-- Message Bubble -->
    <TextView
        android:id="@+id/tvMessage"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:maxWidth="260dp"
        android:padding="10dp"
        android:textSize="14sp"
        android:textColor="#212121"
        android:background="@drawable/bg_chat_bubble_ai"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/ivAvatar"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginStart="8dp" />

    <!-- Timestamp -->
    <TextView
        android:id="@+id/tvTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="10sp"
        android:textColor="#9E9E9E"
        app:layout_constraintTop_toBottomOf="@id/tvMessage"
        app:layout_constraintEnd_toEndOf="@id/tvMessage" />

</androidx.constraintlayout.widget.ConstraintLayout>
